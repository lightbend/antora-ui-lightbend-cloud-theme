language: node_js
node_js: "lts/*"

before_script:
  - npm install -g gulp-cli link-checker

script:
  - gulp preview:build bundle:pack
  - link-checker public

cache:
  yarn: true
  directories:
    - $HOME/.cache
